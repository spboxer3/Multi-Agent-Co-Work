---
name: status
description: Show status for a multi-agent-cowork run.
---

Run this exact command first:

```bash
cd "$PROJECT_DIR" && python "C:/Users/rolandchien/.claude/plugins/marketplaces/Multi-Agent-Co-Work/runtime/maw.py" status --run-id "$ARGUMENTS"
```

Replace `$PROJECT_DIR` with the user's current working project directory.
