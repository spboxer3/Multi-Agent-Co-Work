description = "resume a multi-agent-cowork run"
prompt = """
Run this exact shell command first and wait for it to finish:

!{python3 .agents/multi-agent-cowork/runtime/maw.py resume --run-id "{{args}}"}

Then summarize only the persisted outcome.
"""
