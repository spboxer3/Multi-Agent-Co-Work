description = "persist routing profile or per-phase overrides"
prompt = """
Run this exact shell command first and wait for it to finish:

!{python runtime/maw.py routing set {{args}}}

Then summarize only the persisted outcome.
"""
