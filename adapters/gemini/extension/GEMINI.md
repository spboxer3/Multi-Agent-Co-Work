# Multi-Agent Co-Work

This extension exists so Gemini can call the repository-local orchestrator instead of pretending to be all agents at once.

Use the `/maw:*` commands when the user asks for actual multi-agent collaboration or routing changes.
