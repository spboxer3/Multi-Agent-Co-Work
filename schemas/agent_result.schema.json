{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "status": {"type": "string", "enum": ["ok", "failed", "partial"]},
    "summary": {"type": "string"},
    "relevant_files": {"type": "array", "items": {"type": "string"}},
    "evidence_map": {"type": "array", "items": {"type": "string"}},
    "entrypoint_status": {"type": "string", "enum": ["confirmed", "partial", "unknown"]},
    "entrypoint_direction": {"type": "string"},
    "surface_coverage": {"type": "number", "minimum": 0, "maximum": 1},
    "proposed_changes": {"type": "array", "items": {"type": "string"}},
    "requested_files": {"type": "array", "items": {"type": "string"}},
    "file_set_mode": {"type": "string", "enum": ["locked", "open-with-reason", "unknown"]},
    "verification_commands": {"type": "array", "items": {"type": "string"}},
    "failure_classification": {"type": "array", "items": {"type": "string"}},
    "scope_extension_needed": {"type": "boolean"},
    "scope_extension_reason": {"type": "string"},
    "rollback_anchor": {"type": "string"},
    "commands": {"type": "array", "items": {"type": "string"}},
    "risks": {"type": "array", "items": {"type": "string"}},
    "notes": {"type": "array", "items": {"type": "string"}},
    "open_questions": {"type": "array", "items": {"type": "string"}},
    "blockers": {"type": "array", "items": {"type": "string"}},
    "decision": {"type": "string", "enum": ["continue", "re-explore", "re-plan", "request-scope-extension", "rollback", "block-release"]},
    "confidence": {"type": "number", "minimum": 0, "maximum": 1}
  },
  "required": [
    "status",
    "summary",
    "relevant_files",
    "evidence_map",
    "entrypoint_status",
    "entrypoint_direction",
    "surface_coverage",
    "proposed_changes",
    "requested_files",
    "file_set_mode",
    "verification_commands",
    "failure_classification",
    "scope_extension_needed",
    "scope_extension_reason",
    "rollback_anchor",
    "commands",
    "risks",
    "notes",
    "open_questions",
    "blockers",
    "decision",
    "confidence"
  ]
}
